<script type="text/javascript">
function numbersonly(e,value){
var unicode=e.charCode? e.charCode : e.keyCode
if (unicode!=8){ //if the key isn't the backspace key (which we should allow)
if (unicode<48||unicode>57) //if not a number
return false //disable key press
}
}

function xxx(){
var x=10;
alert();
//var x=document.getElementById('phone_no').value;
alert(x);
if (x==null || x=="")
  {
  alert("First name must be filled out");
  return false;
  }
}
</script>


<h1>Mobile No</h1>
<div id="phone">
    <form name="phone_form" action="/phone" method="post">
<p>(enter and verify your mobile no to receive alert from BillGuru)</p>
	<input id="pnone_no" autocomplete="off" type="text" placeholder="Phone No" onkeypress="return numbersonly(event,this.value)" name="pnone_no" size="15" />
	<input id="phone_button" type="submit" value="Submit"/>
    </form>
<div>

       <% if current_user.token.phone_active %>
	    <div style="padding-top:7px;float:left">Status : Verified </div>
            <div style="padding-top:5px;padding-left:10px;float:left"> <img src="images/icons/true_stat.png" alt="True"/></div>
            <div style="padding-top:7px;padding-left:10px;float:left"> <b>(phone no: <%= current_user.token.phone_no %>)</b> </div>
       <% else %>	  
	    <div style="padding-top:7px;float:left">Status : Not verified</div>
            <div style="padding-top:5px;padding-left:10px;float:left"> <img src="images/icons/false_stat.png" alt="False"/></div>
       <% end %>

</div>
</div>
<br/>
<br/>
<h1>Verify Mobile No</h1>
<div>
    <form name="phone_form" action="/verify_phone" method="get">
<p>(enter the verification code which you received from BillGuru)</p>
	<input id="verify_no" autocomplete="off" type="text" placeholder="Verification Code" name="verify_no" size="15" />
	<input id="phone_button" type="submit" value="Verify" />
    </form>

</div>


